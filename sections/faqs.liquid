<div class="faqs-section">
  {% if section.settings.show_title %}<h2 class="faqs-title">{{ section.settings.title }}</h2>{% endif %}

  <div class="faqs-container">
    {% assign faqs = metaobjects.frequently_asked_questions.values %}
    {% for faq in faqs %}
      <details class="faq-item">
        <summary class="faq-question">
          {{ faq.question.value }}
          <span class="faq-icon">+</span>
        </summary>
        <div class="faq-answer">
          {{ faq.answer.value }}
        </div>
      </details>
    {% endfor %}
  </div>
</div>

<style>
  .faqs-section {
    max-width: 800px;
    margin: 0 auto;
    padding: 10px 20px;
  }

  .faqs-title {
    font-size: 28px;
    margin-bottom: 20px;
  }

  .faq-item {
    border-top: 1px solid #ddd;
    margin-bottom: 10px;
  }

  .faq-question {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 16px;
    cursor: pointer;
    user-select: none;
  }

  .faq-icon {
    font-size: 20px;
    transition: transform 0.3s;
  }

  details[open] .faq-icon {
    transform: rotate(45deg);
  }

  .faq-answer {
    padding: 16px;
    line-height: 1.6;
  }
</style>

{% schema %}
  {
    "name": "FAQs",
    "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "Section Title",
        "default": "Frequently Asked Questions",
        "info": "Show the frequently asked questions that can be edited in their metabject here https://admin.shopify.com/store/hannah-martin-jewellery/content/metaobjects/entries/frequently_asked_questions"
      },
      {
        "type": "checkbox",
        "id": "show_title",
        "label": "Show Title",
        "default":true
      }
    ],
    "presets": [
      {
        "name": "FAQs"
      }
    ]
  }
{% endschema %}